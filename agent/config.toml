# TEST config pointing at the local GreenMail server (see verification).
# For real use, copy config.example.toml and point at Proton Bridge instead.
database_url  = "postgresql+psycopg://meerail:meerail@127.0.0.1:5432/meerail"
tika_url      = "http://127.0.0.1:9998"
poll_interval = 30
batch_size    = 200

[[account]]
email         = "tester@example.com"
imap_host     = "127.0.0.1"
imap_port     = 3143
imap_security = "plain"
smtp_host     = "127.0.0.1"
smtp_port     = 3025
smtp_security = "plain"
username      = "tester@example.com"
password      = "whatever"
verify_cert   = false
