# meerail-server dependencies. The agent has its own agent/requirements.txt;
# both share the `core` package, so keep the overlapping pins in step.
fastapi==0.116.1
# Pinned directly, not left to FastAPI's range. Starlette's multipart parser
# wrote large uploads to disk from the event loop, so one upload stalled every
# other request on the process (CVE-2025-54121); the fix landed in 0.47.2, and
# fastapi 0.115.x capped starlette below 0.42 and could not reach it. Both had
# to move together, and this pin is what keeps a resolver from quietly walking
# back into the range that carries the bug.
starlette==0.47.3
uvicorn[standard]==0.34.0
sqlalchemy==2.0.51
pydantic==2.13.4
pydantic-settings==2.7.0
email-validator==2.2.0
python-multipart==0.0.20
psycopg[binary]==3.3.4      # psycopg3: has wheels for current Pythons (agent runs on the host)

# Mail-specific
nh3==0.2.20                 # HTML sanitization for safe rendering
selectolax==0.4.11          # fast HTML -> text for the search corpus
sse-starlette==2.1.3        # server-sent events (live updates + sync progress)
cryptography==44.0.0        # Fernet, if server-side credential storage is enabled
python-dateutil==2.9.0.post0
httpx==0.28.1               # calls to Apache Tika
